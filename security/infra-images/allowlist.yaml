# Infra-image CVE finding allowlist — feature 035 (see specs/035-infra-image-cve-scan/data-model.md).
#
# Consumed ONLY by the gate (scripts/check-infra-image-findings.mjs). Suppresses a matching finding
# from the blocking gate while it stays VISIBLE in security/infra-images/reports/findings.json. The
# gate fails only on `blocking` findings (FIXABLE Critical — matching the cd-deploy Trivy gate);
# fixable High and everything else (unfixable, Medium, Low) are non-blocking warnings.
#
# Match key (all four required; a blank field or invalid regex is a GATE ERROR):
#   - image:           regex vs Finding.image (the scanned repo:tag)
#     id:              regex vs advisory id (CVE-*/GHSA-*)
#     justification:   why accepted — usually "awaiting Renovate base-image bump to <fixed tag>"
#     addedBy:         who triaged
#     expiry:          optional ISO YYYY-MM-DD — once past, the entry stops suppressing (re-blocks)
#
# ═════════════════════════════════════════════════════════════════════════════════════════════════
# BASELINE (seeded 2026-07-11 from the first Linux/CI Trivy scan — Trivy is not on the Windows dev
# box). Every entry below is a pre-existing FIXABLE Critical present at seeding; `main` is green on
# day one and the gate thereafter blocks only NEW fixable-Critical findings. Remediation is a Renovate
# base-image bump; delete the entry after the bump so a regression re-blocks. Fixable HIGH CVEs (851 at
# seeding) are report-only warnings (base OS images carry hundreds — gating on those is noise; same
# CRITICAL-only policy as cd-deploy). See security/infra-images/reports/findings.json for the full set.
# ═════════════════════════════════════════════════════════════════════════════════════════════════

# caddy: all 5 fixable Criticals (CVE-2025-44005, CVE-2025-68121, CVE-2026-30836, CVE-2026-31789,
# CVE-2026-33186) remediated by bumping 2.10-alpine→2.11.4-alpine (feature 036). Locally Trivy-verified
# clean (smallstep 0.30.2, grpc 1.81.0, Go 1.26.3, libcrypto3 3.5.7-r0). Entries removed; PR re-scans.
# otel-lgtm pgx CVE-2026-33815 / CVE-2026-33816: entries REMOVED 2026-07-26. Grafana rebuilt
# `latest` with pgx/v5 >= 5.9.0, so neither advisory appears on the current image. Deliberately
# removed WITHOUT a local re-scan: Trivy is not available in the dev container (the DinD daemon was
# down), and removal is the fail-SAFE direction — if CI's own pull still carries either CVE, the
# infra-image-scan job on this PR re-blocks the gate and the entries come back with CI evidence
# attached. That PR-side scan IS the CI-side confirmation the follow-up asked for; `security/
# infra-images/**` is in the workflow's path filter, so touching this file triggers a full sweep.
# ── NEW advisories on unchanged pinned images (triaged 2026-07-26, run 1162) ──────────────────────
# All three below are floating/major-only tags that ALREADY resolve to newest, so there is no bump to
# make — the same situation documented for opensearch:2. Remediation is upstream rebuilding the image
# with the fixed dep. Locally Trivy-verified (--severity CRITICAL --ignore-unfixed) on 2026-07-26.
#
# These three carry a 90-day `expiry` (2026-10-24) — a deliberate divergence from the undated seed
# baseline above. They suppress three REAL, unfixed Criticals, and an undated entry means the gate
# stays permanently quiet about them with nothing to force re-triage. Expiry converts silence into a
# deadline: past that date they stop suppressing and the gate re-blocks, which is a prompt to re-check
# whether upstream has shipped the fix — not a claim that the risk is gone. Both remain internal-only
# and dormant by default (`--profile observability`), which is why suppression is defensible at all.
- image: 'grafana/otel-lgtm:latest'
  id: 'GHSA-r277-6w6q-xmqw'
  justification: 'NEW fixable Critical in github.com/getkin/kin-openapi (v0.140.0→0.144.0), bundled by the upstream image. `latest` already floats to newest so no pin/bump clears it — blocked until Grafana rebuilds otel-lgtm with kin-openapi>=0.144.0. Internal-only: observability stack, --profile observability, dormant by default. Expires 2026-10-24 to force re-triage rather than suppressing indefinitely.'
  addedBy: 'triage-2026-07-26'
  expiry: '2026-10-24'
- image: 'hashicorp/vault:1\.18'
  id: 'CVE-2025-68121'
  justification: 'Baseline (035): fixable Critical in stdlib. Bump to 1.21.4 would clear THIS one (Go retoolchained) but NOT the sibling libcrypto3/grpc Crits, and 1.21.4 adds NEW pgx Criticals (Trivy-verified, feature 036) — no fully-clean upstream tag yet; internal-only + dormant. Re-triage when a clean vault tag ships.'
  addedBy: 'seed'
- image: 'hashicorp/vault:1\.18'
  id: 'CVE-2026-31789'
  justification: 'Baseline (035): fixable Critical in libcrypto3. Verified NOT cleared by a bump to 1.21.4 (still ships libcrypto3 3.5.5-r0 < fix 3.5.6-r0; Trivy, feature 036) — no clean upstream tag yet; internal-only + dormant.'
  addedBy: 'seed'
- image: 'hashicorp/vault:1\.18'
  id: 'CVE-2026-33186'
  justification: 'Baseline (035): fixable Critical in google.golang.org/grpc. Verified NOT cleared by a bump to 1.21.4 (still ships grpc v1.75.1 < fix 1.79.3; Trivy, feature 036) — no clean upstream tag yet; internal-only + dormant.'
  addedBy: 'seed'
- image: 'langfuse/langfuse:3'
  id: 'GHSA-7rqj-j65f-68wh'
  justification: 'NEW fixable Critical in next-auth (4.24.14→4.24.15), bundled by the upstream image. The `3` tag is a major-version float already resolving to newest 3.x, so no pin/bump clears it — blocked until LangFuse rebuilds with next-auth>=4.24.15. Internal-only: observability stack, --profile observability, dormant by default; LangFuse auth is not an internet-facing surface here. Expires 2026-10-24 to force re-triage rather than suppressing indefinitely.'
  addedBy: 'triage-2026-07-26'
  expiry: '2026-10-24'
- image: 'langfuse/langfuse-worker:3'
  id: 'GHSA-7rqj-j65f-68wh'
  justification: 'NEW fixable Critical in next-auth (4.24.14→4.24.15) — same upstream dep as langfuse/langfuse:3 above (both images ship it). Major-version float already at newest 3.x, so no pin/bump clears it. Internal-only: observability stack, --profile observability, dormant by default. Expires 2026-10-24 to force re-triage rather than suppressing indefinitely.'
  addedBy: 'triage-2026-07-26'
  expiry: '2026-10-24'
- image: 'minio/mc'
  id: 'CVE-2025-68121'
  justification: 'Baseline (035): pre-existing fixable Critical in stdlib (v1.24.6→1.24.13, 1.25.7, 1.26.0-rc.3) — awaiting Renovate base-image bump.'
  addedBy: 'seed'
- image: 'minio/mc'
  id: 'CVE-2026-33186'
  justification: 'Baseline (035): pre-existing fixable Critical in google.golang.org/grpc (v1.71.0→1.79.3) — awaiting Renovate base-image bump.'
  addedBy: 'seed'
- image: 'minio/minio'
  id: 'CVE-2025-68121'
  justification: 'Baseline (035): pre-existing fixable Critical in stdlib (v1.24.6→1.24.13, 1.25.7, 1.26.0-rc.3) — awaiting Renovate base-image bump.'
  addedBy: 'seed'
- image: 'minio/minio'
  id: 'CVE-2026-33186'
  justification: 'Baseline (035): pre-existing fixable Critical in google.golang.org/grpc (v1.71.0→1.79.3) — awaiting Renovate base-image bump.'
  addedBy: 'seed'
- image: 'mongodb/mongodb-community-server:8\.0\.8-ubi9'
  id: 'CVE-2025-68121'
  justification: 'Baseline (035): fixable Critical in Go stdlib. Kept on 8.0.8 — feature 036 tried 8.0.26 but it (a) still bundles a pre-fix Go 1.25.5 binary (< fix 1.25.7; Trivy-verified) so it does NOT clear this Critical, and (b) came up UNHEALTHY on the CI rootless runner, breaking bring-up. No net benefit → stay on the long-stable 8.0.8. Internal-only; re-triage when a Go≥1.25.7 mongo image ships.'
  addedBy: 'seed'
- image: 'opensearchproject/opensearch:2'
  id: 'CVE-2025-14813'
  justification: 'Baseline (035): fixable Critical in org.bouncycastle:bcprov-jdk15to18 (1.79→1.84). Verified NOT cleared by the latest 2.x (2.19.6): the image STILL bundles bcprov-jdk15to18-1.79.jar alongside the newer bcprov-jdk18on-1.84.jar (Trivy, feature 036). `opensearch:2` already floats to 2.19.6, so pinning does not help — blocked until upstream drops the old jar. Internal-only (audit stack).'
  addedBy: 'seed'
- image: 'postgres:18\.3-alpine3\.23'
  id: 'CVE-2025-68121'
  justification: 'Baseline (035): fixable Critical in Go stdlib. Kept on 18.3 — feature 036 verified the newest 18.4-alpine3.24 STILL bundles a pre-fix Go 1.24.6 binary (< fix 1.24.13; Trivy), so a bump does NOT clear this Critical (report-only-High reduction only). Reverted with the mongo bump to keep the data-tier on its long-stable pins. Internal-only.'
  addedBy: 'seed'
- image: 'postgres:16-alpine'
  id: 'CVE-2025-68121'
  justification: 'Baseline (035): fixable Critical in Go stdlib. Kept on 16-alpine — feature 036 verified the newest 16.14-alpine3.24 STILL bundles a pre-fix Go 1.24.6 binary (< fix 1.24.13; Trivy), so pinning/bumping does NOT clear this Critical. Internal-only.'
  addedBy: 'seed'
# CVE-2025-14813 (bcprov-jdk18on 1.82→1.84) remediated by bumping Keycloak 26.5.5→26.7.0 (feature 036).
# Entry removed: the 26.5.5 ref no longer exists. The infra-image-scan CI run on this PR re-scans
# 26.7.0 — if any fixable Critical remains on the new tag it is un-allowlisted and re-blocks the gate.
