{
  "name": "infrastructure-as-code",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "up-auth": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p auth -f infrastructure-as-code/docker/stacks/auth.compose.yaml -f infrastructure-as-code/docker/keycloak/compose.dev.yaml --env-file infrastructure-as-code/docker/stacks/auth.env up -d",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Bring up the auth stack (keycloak-service + keycloak-store-postgres + keycloak-mailpit). The compose.dev.yaml overlay (feature 039) seeds the grumpyrobot realm on a fresh volume via --import-realm. Run `node scripts/gen-dev-secrets.mjs` first (mints auth.env incl. the realm client secrets). Add --args=--profile=vault to include vault-service (prod posture)."
      }
    },
    "up-mcm": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p mcm -f infrastructure-as-code/docker/stacks/mcm.compose.yaml --env-file infrastructure-as-code/docker/stacks/mcm.env --profile app up -d",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Bring up the mcm stack test infra + mc-service (--profile app). Run `node scripts/gen-dev-secrets.mjs` first (mints mcm.env). Bring up auth first (manual ordering). Add bff-nonsecure / bff-secure / agents via docker compose --profile."
      }
    },
    "up-mcm-agents": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/up-mcm-agents.mjs",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Bring up the AI agent layer under the mcm compose stack (HEAVY variant: gateway + 3 MCP + movie-assistant-store-postgres checkpointer). Fetches the agent-gateway Keycloak client secret and injects it so tool calls work. Prereqs: up-auth + up-mcm + host Ollama. Distinct from up-agents-prod (the light docker-run/MemorySaver E2E variant)."
      }
    },
    "up-audit": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p audit -f infrastructure-as-code/docker/stacks/audit.compose.yaml --env-file infrastructure-as-code/docker/stacks/audit.env --profile audit up -d",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Bring up the audit stack (agent-audit-opensearch append-only sink). Run `node scripts/gen-dev-secrets.mjs` first (mints audit.env)."
      }
    },
    "up-observability": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p observability -f infrastructure-as-code/docker/stacks/observability.compose.yaml --env-file infrastructure-as-code/docker/stacks/observability.env --profile observability up -d",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Bring up the observability stack (LangFuse + otel-lgtm + opa-service + unleash-service). Run `node scripts/gen-dev-secrets.mjs` first (mints observability.env)."
      }
    },
    "up-all": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "docker compose -p auth -f infrastructure-as-code/docker/stacks/auth.compose.yaml -f infrastructure-as-code/docker/keycloak/compose.dev.yaml --env-file infrastructure-as-code/docker/stacks/auth.env up -d",
          "docker compose -p mcm -f infrastructure-as-code/docker/stacks/mcm.compose.yaml --env-file infrastructure-as-code/docker/stacks/mcm.env --profile app up -d"
        ],
        "parallel": false,
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Convenience: bring up auth (Keycloak) then the mcm app stack in order (auth before mc-service, FR-006)."
      }
    },
    "down-auth": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p auth -f infrastructure-as-code/docker/stacks/auth.compose.yaml --env-file infrastructure-as-code/docker/stacks/auth.env --profile vault down",
        "cwd": "{workspaceRoot}"
      }
    },
    "down-mcm": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p mcm -f infrastructure-as-code/docker/stacks/mcm.compose.yaml --env-file infrastructure-as-code/docker/stacks/mcm.env --profile app --profile bff-nonsecure --profile bff-secure --profile agents --profile agents-metro down",
        "cwd": "{workspaceRoot}"
      }
    },
    "down-audit": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p audit -f infrastructure-as-code/docker/stacks/audit.compose.yaml --env-file infrastructure-as-code/docker/stacks/audit.env --profile audit down",
        "cwd": "{workspaceRoot}"
      }
    },
    "down-observability": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -p observability -f infrastructure-as-code/docker/stacks/observability.compose.yaml --env-file infrastructure-as-code/docker/stacks/observability.env --profile observability down",
        "cwd": "{workspaceRoot}"
      }
    },
    "down-all": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "docker compose -p mcm -f infrastructure-as-code/docker/stacks/mcm.compose.yaml --env-file infrastructure-as-code/docker/stacks/mcm.env --profile app --profile bff-nonsecure --profile bff-secure --profile agents --profile agents-metro down",
          "docker compose -p auth -f infrastructure-as-code/docker/stacks/auth.compose.yaml --env-file infrastructure-as-code/docker/stacks/auth.env --profile vault down",
          "docker compose -p audit -f infrastructure-as-code/docker/stacks/audit.compose.yaml --env-file infrastructure-as-code/docker/stacks/audit.env --profile audit down",
          "docker compose -p observability -f infrastructure-as-code/docker/stacks/observability.compose.yaml --env-file infrastructure-as-code/docker/stacks/observability.env --profile observability down"
        ],
        "parallel": false,
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Tear down all four stacks (keeps external volumes)."
      }
    },
    "ps": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker ps --format 'table {{.Names}}\\t{{.Status}}'",
        "cwd": "{workspaceRoot}"
      }
    },
    "up-agents-prod": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/agent-stack.mjs",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Deploy the containerized production-node agent stack (gateway + movie-mcp + web-api-mcp; host Ollama, MemorySaver) for local agent E2E. Rebuilds the 4 agent/MCP images from the checkout by default (feature 041 \u2014 skipping made CI test stale images); pass --args=--no-build for a fast local loop (refused under CI)."
      }
    },
    "down-agents-prod": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/agent-stack.mjs --down",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Remove the containerized production-node agent stack (gateway + movie-mcp + web-api-mcp)."
      }
    },
    "status-agents-prod": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/agent-stack.mjs --status",
        "cwd": "{workspaceRoot}"
      }
    },
    "check-naming": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/check-resource-naming.mjs",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Static resource-naming gate (features 019 + 020): assert every Docker volume/network name, and every container_name == service key == convention. Pass --args=--section=volumes|networks|containers|ollama|all (default all)."
      }
    },
    "check-no-inline-secrets": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/check-no-inline-secrets.mjs",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Static inline-secret gate (feature 021): assert no tracked compose file holds a clear-text credential \u2014 every secret-shaped key / password URL / auth literal must be a ${VAR} reference. Pass --args=--selftest to validate the detector."
      }
    },
    "dast": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/zap-scan.mjs --target local --mode baseline",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Run the OWASP ZAP DAST baseline (non-destructive: spider + passive) against the local stack, authenticated as the E2E test user. Reports \u2192 security/zap/reports/. Prereqs: auth + mcm stacks up (add --profile agents for the gateway target). Pass --args='--mode full' + DAST_ALLOW_ACTIVE=1 for the destructive active scan (disposable stacks only, FR-017). See docs/runbooks/dast-scanning.md."
      }
    },
    "sast": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/sast-scan.mjs --scope full",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Run the keyless SAST + SCA static scan (Semgrep code SAST over TS/JS + Python; cargo/pnpm/pip audit over the three dependency graphs) and write a consolidated, risk-normalized report to security/sast/reports/. No running app stack required. Prereqs: uv/uvx, a Rust toolchain with cargo-audit, pnpm. Pass --args='--scope changed --base <ref>' to scope Semgrep to changed files (SCA still runs full). Gate the report with `node scripts/check-sast-findings.mjs`. See docs/runbooks/sast-scanning.md."
      }
    },
    "wiki-update": {
      "executor": "nx:run-commands",
      "options": {
        "command": "openwiki code --update --print",
        "cwd": "{workspaceRoot}",
        "env": {
          "OPENWIKI_PROVIDER": "anthropic",
          "OPENWIKI_MODEL_ID": "claude-sonnet-5",
          "OPENWIKI_TELEMETRY_DISABLED": "1",
          "NODE_OPTIONS": "--max-old-space-size=8192"
        }
      },
      "metadata": {
        "description": "Generate or refresh the OKF knowledge bundle at openwiki/ (feature 043). ALWAYS invoke through this target, never the bare CLI: it sets OPENWIKI_TELEMETRY_DISABLED=1, and the tool reports usage telemetry to a third-party analytics host by DEFAULT (FR-030). The dev container's egress allowlist would block that, but the Windows host has no such protection — so the opt-out lives here, in the invocation path, rather than in setup docs a developer could skip. Uses --update (not --init): it creates the bundle when absent, so the interactive onboarding and the ~/.openwiki/.env it would write are avoided entirely. Scope and redaction rules are hand-authored in openwiki/INSTRUCTIONS.md, which the tool reads but never rewrites. Gate the result with `pnpm nx okf-lint infrastructure-as-code`. See docs/runbooks/devcontainer.md."
      }
    },
    "wiki-plan": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/wiki-maintain.mjs --plan",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Plan OpenWiki maintenance (feature 044) WITHOUT spending anything: decompose the documentation changes since the last recorded run into bounded slices of at most 8 pages, and print them for review. Offline, keyless, free and always safe to run — it makes no model call, so there is never a reason to skip it before `wiki-maintain`. Pass --args=--json for machine-readable output, or --args='--since <ref>' to override the run-record marker. Exit 0 always (2 on bad usage or unreadable state). See docs/runbooks/wiki-maintenance.md."
      }
    },
    "wiki-maintain": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/wiki-maintain.mjs --execute",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Execute planned OpenWiki maintenance (feature 044) — PAID: each slice invokes the generator through `wiki-update`, which needs ANTHROPIC_API_KEY. ALWAYS invoke through this target rather than calling the generator yourself: it verifies every slice by the pages that actually landed in the working tree and by bundle conformance, never by the generator's exit status (feature 043 measured 12 minutes of paid work, one index.md written, exit 0, reported as success). Bounded by a 16-page + 20-minute budget, effective ceiling ≤24 pages / ~37 min once the non-interruptible one-slice overshoot is counted; NEITHER budget is a monetary bound (the generator reports no token or cost data). Exit 1 = a slice failed verification; exit 3 = stopped at the budget with work carried forward, which is NOT a failure. Run `wiki-plan` first. See docs/runbooks/wiki-maintenance.md."
      }
    },
    "okf-lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/check-openwiki-okf.mjs",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Run the OKF conformance gate over the openwiki/ bundle (feature 043): front matter parses, required `type` present, ISO-8601 timestamps, repo-relative `resource` links resolve, an index.md per directory, no orphaned concepts. Offline and keyless — external links are shape-checked, never fetched. Fail-closed: an absent or partially-written bundle fails rather than passing vacuously, and there is no skip flag. Drift (a source newer than the concept citing it) is reported as a WARNING and never affects the exit code. Pass --args=--selftest to validate the detector first."
      }
    },
    "okf-governance": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/check-openwiki-governance.mjs",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Run the OpenWiki governance gate over this repository (feature 044): every documentation path carries a declared regeneration policy, `actor: generator` appears only inside openwiki/, every event-driven entry names its events, every relocated load-bearing passage still matches its fingerprint (a REMOVED one fails as a removal, not as nothing-to-compare), no protected passage sits on a concept that cites a source, CLAUDE.md holds only its index and its three machine-managed regions, every index entry and assistant-surface link resolves, and every concept is provably exactly one of derived or authoritative. Offline, keyless, fail-closed \u2014 a missing or unparseable governance file is a violation, and there is deliberately NO skip flag and no allowlist: a rejected page is fixed in openwiki/INSTRUCTIONS.md and regenerated. Pass --args=--selftest to validate the detector first, --args='--explain <path>' to see which policy entry governs a path, or --args='--fingerprint <concept> \"<anchor>\"' to compute the value protected.yaml should carry. See docs/runbooks/wiki-maintenance.md."
      }
    },
    "infra-scan": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/infra-image-scan.mjs",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Run the keyless infra-image CVE scan (feature 035): enumerate the third-party images pulled in infrastructure-as-code/** (excluding the six jumbleknot/* images cd-deploy already scans, and ${..}-interpolated refs), scan each with Trivy, and write a normalized report to security/infra-images/reports/. Prereq: Trivy on PATH + Docker (Linux/CI \u2014 not the Windows dev box). Pass --args='--list' to enumerate only (no Trivy). Gate the report with `node scripts/check-infra-image-findings.mjs`. See docs/runbooks/infra-image-scanning.md."
      }
    },
    "build-devcontainer-image": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/build-devcontainer-image.mjs",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Feature 038 \u2014 build the heavy dev-container toolchain image locally from .devcontainer/toolchain.Dockerfile and tag it mcm-devcontainer:local (the offline / no-forge one-time fallback that makes the devcontainer.json BASE_IMAGE default resolve; SC-011). The fast path is instead pulling the CI-published forge image via MCM_DEVCONTAINER_IMAGE. Prereq: Docker. Pass --args='--tag <ref>' to override the tag. See docs/runbooks/devcontainer.md."
      }
    },
    "preflight": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/preflight.mjs",
        "cwd": "{workspaceRoot}"
      },
      "metadata": {
        "description": "Run every CHEAP check CI runs, before pushing: all guardrail gates (selftest + scan), the scripts/__tests__ unit tests, then lint + typecheck + unit for mcm-app. Deliberately excludes app-e2e, dast, the integration tiers and the SAST/SCA scan \u2014 those need the CI runner, a live stack or an emulator. Exists because CI has ONE runner and app-e2e is ~35 min, so a red push for an offline-knowable reason is disproportionately expensive. Pass --args='--gates-only' to skip lint/typecheck/unit. See CLAUDE.md > Test Run Protocol."
      }
    }
  }
}
