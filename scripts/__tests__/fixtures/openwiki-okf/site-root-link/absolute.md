---
type: Gotcha
title: Absolute
description: Every rule but V14 is satisfied here.
resource: README.md
---
The target below is a real repository path and `resource` above resolves — which is exactly the
state 204 dead links sat in on `main`. Only the leading `/` is wrong.

See [the generation brief](/openwiki/INSTRUCTIONS.md).
